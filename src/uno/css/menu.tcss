#menu-layout {
    width: 100%;
    height: 1fr;
    align: center middle;
}

#logo {
    width: auto;
    text-align: center;
    margin-bottom: 1;
}

#version-label {
    text-align: center;
    color: darkgrey;
    text-style: italic;
    margin-bottom: 2;
}

#main-menu {
    width: 30;
    height: auto;
    border: none;
}
